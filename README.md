MultipartSplit
==============

MultipartSplit Plugin for [QGIS](www.qgis.org)

The plugins adds a "Splits feature(s) parts" item to the Edit menu, and a similar button in the Advanced digitizing toolbar, allowing to convert multipart features into single part feature in the fly. The buttons are only enabled when the current layer is in edit mode and has features selected.
